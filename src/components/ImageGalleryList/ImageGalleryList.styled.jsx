import styled from 'styled-components';

export const PhotoList = styled.ul`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(0px, 376px));
  grid-gap: 16px;

  margin: 0 auto 30px;
`;
